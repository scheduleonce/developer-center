---
title: API Reference Explorer
slug: /scalar
---

import ScalarExplorer from "@site/src/components/ScalarExplorer";

<ScalarExplorer />

If the explorer does not appear, open DevTools console (look for [ScalarExplorer] logs). It falls back to the bundled /openapi.json if the remote spec is unreachable.
