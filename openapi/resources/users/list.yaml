users:
  get:
    tags:
      - "Users"
    summary: List all users
    description: Returns a list of all users in your account
    operationId: list-all-users
    parameters:
      - $ref: "../../components/parameters/common.yaml#/Email"
      - $ref: "../../components/parameters/common.yaml#/Before"
      - $ref: "../../components/parameters/common.yaml#/After"
      - $ref: "../../components/parameters/common.yaml#/Limit"
    responses:
      "200":
        description: "200"
        content:
          application/json:
            schema:
              $ref: "../../components/schemas/user.yaml#/UserList"
      "401":
        $ref: "../../components/responses/errors.yaml#/Unauthorized"
    deprecated: false
