event-types:
  get:
    tags:
      - "Event Types"
    summary: List all event types
    description: Returns a list of all event types in your account
    operationId: list-all-event-types
    parameters:
      - $ref: "../../components/parameters/common.yaml#/Before"
      - $ref: "../../components/parameters/common.yaml#/After"
      - $ref: "../../components/parameters/common.yaml#/Limit"
    responses:
      "200":
        description: "200"
        content:
          application/json:
            schema:
              $ref: "../../components/schemas/event-type.yaml#/EventTypeList"
      "401":
        $ref: "../../components/responses/errors.yaml#/Unauthorized"
    deprecated: false
